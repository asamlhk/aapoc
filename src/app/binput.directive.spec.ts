import { BinputDirective } from './binput.directive';

describe('BinputDirective', () => {
  it('should create an instance', () => {
    const directive = new BinputDirective();
    expect(directive).toBeTruthy();
  });
});
